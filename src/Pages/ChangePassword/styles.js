import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: "15%",
    alignItems: "center",
  },
  text: {
    fontSize: 14,
    fontWeight: "500",
    marginBottom: 20,
  },
});
